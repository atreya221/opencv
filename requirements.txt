################################################################

<<<<<<< HEAD
# run the setup.py file to install the required dependancies.
=======
# For Windows users, run the following commands :

# 1. Make sure you have python/python3  and pip installed and can be executed using respective commands from CMD.
# 2. cd "path/to/project/directory/".
# 3. Run "python -m pip install virtualenv" or "python3 -m pip install virtualenv". (if virtualenv module is not already installed)
# 4. Activate virtual environment using ".\opencv1\Scripts\activate".
# 5. Install the necessary modules using "pip install -r requirements.txt".

# For Linux/MacOS users, run the following commands :

# 1. Make sure you have python/python3 installed and can be executed using respective commands from bash.
# 2. cd "path/to/project/directory/".
# 3. Run "python -m pip install --user virtualenv" or "python3 -m pip install --user virtualenv". (if virtualenv module is not already installed)
# 4. Run "source opencv2/bin/activate" to activate the virtual environment.
# 5. Install the necessary modules using "pip install -r requirements.txt".
>>>>>>> 66584976794e20174178a91af1f25d241a92fc9b

################################################################

################################################################

# Python package requirements for the project:

tensorflow
opencv-python
matplotlib
pyfastcopy

################################################################
